# NGINX configuration for API gateway and load balancing

# Define upstreams for each backend microservice
upstream user_servers {
    server user-service:8000;
}

upstream hw_servers {
    server hw-service-1:8000;
    server hw-service-2:8000;
}

upstream notification_servers {
    server notification-service:8000;
}

server {
    listen 80;

    # Route /users/* requests to user-service
    location /users/ {
        proxy_pass http://user_servers;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Route /homework/* requests to hw-service
    location /homework/ {
        proxy_pass http://hw_servers;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Route /notifications/* requests to notification-service
    location /notifications/ {
        proxy_pass http://notification_servers;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # simple gateway health
    location = /health {
        return 200 "ok";
        add_header Content-Type text/plain;
    }
}